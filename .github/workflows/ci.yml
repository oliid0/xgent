name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

env:
  NODE_VERSION: 22.19.0

jobs:
  gateway:
    name: Gateway
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          # buf breaking 需要完整历史以对比 origin/main 的 proto 契约。
          fetch-depth: 0

      - uses: actions/setup-go@v5
        with:
          go-version-file: crates/gateway/go.mod
          cache-dependency-path: crates/gateway/go.sum

      - name: Install buf
        uses: bufbuild/buf-action@v1
        with:
          setup_only: true
          version: 1.71.0

      - name: Install protobuf Go plugins
        run: |
          go install google.golang.org/protobuf/cmd/protoc-gen-go@v1.36.11
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"

      - name: Proto lint and breaking-change check
        run: make proto-check BUF_BREAKING_AGAINST='../../.git#branch=origin/main,subdir=crates/gateway'

      - name: Check generated protobuf
        run: |
          make proto
          git diff --exit-code -- crates/gateway/internal/proto

      - name: Lint gateway
        uses: golangci/golangci-lint-action@v8
        with:
          version: v2.12.2
          working-directory: crates/gateway

      - name: Test gateway
        working-directory: crates/gateway
        run: go test ./...

  frontend:
    name: Unified Frontend
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install pnpm
        run: npm install -g pnpm@10.32.1

      - name: Install dependencies
        working-directory: crates/fronted
        run: pnpm install --frozen-lockfile

      - name: Typecheck and build Web frontend
        working-directory: crates/fronted
        run: pnpm build:web

      - name: Lint unified frontend
        working-directory: crates/fronted
        run: pnpm lint

      - name: Test frontend modules
        working-directory: crates/fronted
        run: pnpm test:frontend

      - name: Test release scripts
        working-directory: crates/fronted
        run: pnpm test:release

  tauri-rust:
    name: Tauri Rust Check
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v6

      - name: Install Tauri Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            protobuf-compiler \
            libwebkit2gtk-4.1-dev \
            libgtk-3-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: crates/fronted/src-tauri

      - name: Check Tauri backend tests
        env:
          CARGO_TERM_COLOR: always
        run: cargo check --manifest-path crates/fronted/src-tauri/Cargo.toml --tests

      - name: Test Tauri history migrations
        env:
          CARGO_TERM_COLOR: always
        run: cargo test --manifest-path crates/fronted/src-tauri/Cargo.toml chat_history --lib

  architecture:
    name: Architecture Guard
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Check single-frontend boundaries
        run: node scripts/check-architecture.mjs

  whitespace:
    name: Diff Hygiene
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: git diff --check
