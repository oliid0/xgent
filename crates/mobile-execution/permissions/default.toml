"$schema" = "schemas/schema.json"

[default]
description = "Allows XAgent's bundled mobile execution environment to be inspected, installed, run, and cancelled."
permissions = [
  "allow-status",
  "allow-install",
  "allow-install-toolchains",
  "allow-run",
  "allow-cancel",
]
