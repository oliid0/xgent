"$schema" = "schemas/schema.json"

[default]
description = "Allows XAgent to create isolated browser sessions and operate them on the user's behalf."
permissions = [
  "allow-status",
  "allow-open-session",
  "allow-list-sessions",
  "allow-close-session",
  "allow-set-viewport",
  "allow-action",
]
